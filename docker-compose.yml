services:
  app:
    build: .
    volumes:
      - ./wordlists:/app/wordlists
      - ./csv_data:/app/csv_data
    depends_on:
      - redis
  redis:
    image: "redis:alpine"
    volumes:
      - redis_data:/data
volumes:
  redis_data: